<!DOCTYPE html>
<html dir="rtl">
<head>
    <meta charset="utf-8">
    <title>{{ $subject }}</title>
</head>
<body>
    <h2>{{ $greeting }}</h2>

    <p>{{ $intro }}</p>

    <p style="white-space: pre-wrap;">{{ $content }}</p>

    <p>{{ $farewell }}</p>

    <p>{{ $signature }}</p>
</body>
</html>
